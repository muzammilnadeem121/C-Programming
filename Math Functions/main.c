#include <stdio.h>
#include <math.h>

int main(){

    int x = 9;
    float PI = 3.14;
    int y = -7;

    // x = sqrt(x);
    // x = pow(x, 2);
    // PI = round(PI); round off to the closest integer
    // PI = ceil(PI); round off to the next integer
    // PI = floor(PI); round off to the previous integer
    // y = abs(y);
    // PI = log(PI);
    // x = sin(x); takes value in radians
    // x = cos(x);
    // x = tan(x);

    printf("%d\n", x);
    printf("%.2f\n", PI);
    printf("%d", y);

    return 0;
}